# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:$HOME/bin

export PATH
export PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/go/bin:/usr/local/go/bin:/root/bin:/root/bin:/root/bin
